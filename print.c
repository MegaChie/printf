#include "main.h"
/**
 * _printf - A replica of printf (hopfully)
 * @format: arguments of function
 */
int _printf(const char *format, ...);
{
	int lettersCount, i;
	char toPrint;

	if (format == 0)
	{
		return (-1);
	}
	va_start(list, format)
	for (i = 0; format && format[i] != '\0'; i++)
	{
		
	}
}
